<h2>Editing Book</h2>

  <% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this obj from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  
  <%= form_with model: @book do |f| %>
  
  <h4>Title</h4>
    <%= f.text_field :title %>
      
  <h4>Opinion</h4>
    <%= f.text_area :body %>
  <%= link_to book_path(book_user), class: "btn btn-success" do %>Update Book
  <% end %>  

  <div>
    <%= link_to "Show", book_path(@book.id) %>
    <a href='/books'>Back</a>
  </div>
    
  <% end %>